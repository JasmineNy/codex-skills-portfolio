# Template assets

The original branded PowerPoint templates and the derived data-expression index are intentionally excluded from the public portfolio because they may contain proprietary brand assets.

To use this Skill, add locally approved templates named `ppt模版01.pptx` and `ppt模版02.pptx`, then run `scripts/rebuild-data-expression-index.mjs` if an index is required.
